import { Routes } from '@angular/router';
import { WikiDemoComponent } from './wiki-demo.component';

/** Routes de la page de démonstration du wiki */
export const wikiDemoRoutes: Routes = [
  { path: '', component: WikiDemoComponent },
];

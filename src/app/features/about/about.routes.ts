import { Routes } from '@angular/router';
import { AboutComponent } from './about.component';

/**
 * Routes de la page À propos.
 */
export const aboutRoutes: Routes = [
  { path: '', component: AboutComponent },
];
